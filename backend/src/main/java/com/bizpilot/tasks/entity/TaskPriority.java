package com.bizpilot.tasks.entity;

/** Fixed by CLAUDE.md §23 — exactly these 4 values, no others. Defaults to {@link #MEDIUM} when omitted. */
public enum TaskPriority {
    LOW,
    MEDIUM,
    HIGH,
    URGENT
}
