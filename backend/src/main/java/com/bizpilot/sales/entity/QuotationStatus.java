package com.bizpilot.sales.entity;

/** Fixed by CLAUDE.md §14 — exactly these 6 values, no others. */
public enum QuotationStatus {
    DRAFT,
    SENT,
    ACCEPTED,
    REJECTED,
    EXPIRED,
    CANCELLED
}
