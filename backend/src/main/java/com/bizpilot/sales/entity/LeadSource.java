package com.bizpilot.sales.entity;

/** Fixed by CLAUDE.md §11 — exactly these 6 values, no others. */
public enum LeadSource {
    WEBSITE,
    REFERRAL,
    SOCIAL_MEDIA,
    EMAIL,
    PHONE,
    OTHER
}
